<Application x:Class="GravityCapture.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="MainWindow.xaml">
    <Application.Resources>

        <!-- Palette -->
        <Color x:Key="BgTop">#0A0F16</Color>
        <Color x:Key="BgBottom">#070A10</Color>
        <Color x:Key="PanelBg">#121826</Color>
        <Color x:Key="CardBg">#0F1522</Color>
        <Color x:Key="Stroke">#223049</Color>
        <Color x:Key="StrokeSoft">#1A2538</Color>
        <Color x:Key="TextPrimary">#E9EEF7</Color>
        <Color x:Key="TextSecondary">#A8B4C8</Color>
        <Color x:Key="Accent">#79B0FF</Color>
        <Color x:Key="Danger">#FF6B6B</Color>

        <SolidColorBrush x:Key="BrushTextPrimary" Color="{StaticResource TextPrimary}"/>
        <SolidColorBrush x:Key="BrushTextSecondary" Color="{StaticResource TextSecondary}"/>
        <SolidColorBrush x:Key="BrushStroke" Color="{StaticResource Stroke}"/>
        <SolidColorBrush x:Key="BrushStrokeSoft" Color="{StaticResource StrokeSoft}"/>
        <SolidColorBrush x:Key="BrushPanelBg" Color="{StaticResource PanelBg}"/>
        <SolidColorBrush x:Key="BrushCardBg" Color="{StaticResource CardBg}"/>
        <SolidColorBrush x:Key="BrushAccent" Color="{StaticResource Accent}"/>
        <SolidColorBrush x:Key="BrushDanger" Color="{StaticResource Danger}"/>

        <!-- Default text -->
        <Style TargetType="{x:Type TextBlock}">
            <Setter Property="Foreground" Value="{StaticResource BrushTextPrimary}"/>
        </Style>

        <!-- TextBox / PasswordBox -->
        <Style TargetType="{x:Type TextBox}">
            <Setter Property="Background" Value="#0C111B"/>
            <Setter Property="Foreground" Value="{StaticResource BrushTextPrimary}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BrushStrokeSoft}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="MinHeight" Value="42"/>
            <Setter Property="MinWidth" Value="120"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto"/>
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Hidden"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TextBox}">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="10">
                            <ScrollViewer x:Name="PART_ContentHost"
                                          HorizontalScrollBarVisibility="{TemplateBinding ScrollViewer.HorizontalScrollBarVisibility}"
                                          VerticalScrollBarVisibility="{TemplateBinding ScrollViewer.VerticalScrollBarVisibility}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="{x:Type PasswordBox}">
            <Setter Property="Background" Value="#0C111B"/>
            <Setter Property="Foreground" Value="{StaticResource BrushTextPrimary}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BrushStrokeSoft}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type PasswordBox}">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="10">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Buttons -->
        <Style TargetType="{x:Type Button}">
            <Setter Property="Background" Value="{StaticResource BrushCardBg}"/>
            <Setter Property="Foreground" Value="{StaticResource BrushTextPrimary}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BrushStrokeSoft}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="16,11"/>
            <Setter Property="FontSize" Value="13"/>
                        <Setter Property="MinHeight" Value="42"/>
            <Setter Property="MinWidth" Value="120"/>
<Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#1A2230"/>
                                <Setter Property="BorderBrush" Value="{StaticResource BrushStroke}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#131A25"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButton" TargetType="{x:Type Button}" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="{StaticResource BrushCardBg}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BrushStrokeSoft}"/>
                    <Setter Property="BorderThickness" Value="2"/>
        </Style>

        <Style x:Key="DangerButton" TargetType="{x:Type Button}" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="{StaticResource BrushCardBg}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BrushStrokeSoft}"/>
                    <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <!-- CheckBox -->
        <Style TargetType="{x:Type CheckBox}">
            <Setter Property="Foreground" Value="{StaticResource BrushTextPrimary}"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type CheckBox}">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <Border x:Name="Box" Width="18" Height="18" CornerRadius="5"
                                    Background="#0C111B" BorderBrush="{StaticResource BrushStrokeSoft}" BorderThickness="1"
                                    Margin="0,0,10,0">
                                <Path x:Name="Tick" Data="M 3 9 L 7 13 L 15 4" Stroke="{StaticResource BrushAccent}" StrokeThickness="2"
                                      Visibility="Collapsed"/>
                            </Border>
                            <ContentPresenter VerticalAlignment="Center"/>
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Tick" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="Box" Property="BorderBrush" Value="{StaticResource BrushStroke}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    <!-- Dark scrollbars (global) -->
    <SolidColorBrush x:Key="GC_ScrollBarTrack" Color="#0C111B"/>
    <SolidColorBrush x:Key="GC_ScrollBarThumb" Color="#223049"/>
    <SolidColorBrush x:Key="GC_ScrollBarThumbHover" Color="#2B3D5C"/>
    <SolidColorBrush x:Key="GC_ScrollBarThumbPressed" Color="#1A2538"/>

    <ControlTemplate x:Key="GC_ScrollBarThumbTemplate" TargetType="{x:Type Thumb}">
        <Border x:Name="ThumbBorder"
                Background="{StaticResource GC_ScrollBarThumb}"
                CornerRadius="6"
                SnapsToDevicePixels="True"/>
        <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="ThumbBorder" Property="Background" Value="{StaticResource GC_ScrollBarThumbHover}"/>
            </Trigger>
            <Trigger Property="IsDragging" Value="True">
                <Setter TargetName="ThumbBorder" Property="Background" Value="{StaticResource GC_ScrollBarThumbPressed}"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <ControlTemplate x:Key="GC_ScrollBar_Vertical" TargetType="{x:Type ScrollBar}">
        <Grid SnapsToDevicePixels="True" Width="10" Margin="2,0,0,0">
            <Border Background="{StaticResource GC_ScrollBarTrack}" CornerRadius="6"/>
            <Track Name="PART_Track" IsDirectionReversed="True">
                <Track.DecreaseRepeatButton>
                    <RepeatButton Command="ScrollBar.PageUpCommand" Opacity="0" IsHitTestVisible="False"/>
                </Track.DecreaseRepeatButton>
                <Track.Thumb>
                    <Thumb Template="{StaticResource GC_ScrollBarThumbTemplate}" MinHeight="24" Margin="1"/>
                </Track.Thumb>
                <Track.IncreaseRepeatButton>
                    <RepeatButton Command="ScrollBar.PageDownCommand" Opacity="0" IsHitTestVisible="False"/>
                </Track.IncreaseRepeatButton>
            </Track>
        </Grid>
    </ControlTemplate>

    <ControlTemplate x:Key="GC_ScrollBar_Horizontal" TargetType="{x:Type ScrollBar}">
        <Grid SnapsToDevicePixels="True" Height="10" Margin="0,2,0,0">
            <Border Background="{StaticResource GC_ScrollBarTrack}" CornerRadius="6"/>
            <Track Name="PART_Track" IsDirectionReversed="False" Orientation="Horizontal">
                <Track.DecreaseRepeatButton>
                    <RepeatButton Command="ScrollBar.PageLeftCommand" Opacity="0" IsHitTestVisible="False"/>
                </Track.DecreaseRepeatButton>
                <Track.Thumb>
                    <Thumb Template="{StaticResource GC_ScrollBarThumbTemplate}" MinWidth="24" Margin="1"/>
                </Track.Thumb>
                <Track.IncreaseRepeatButton>
                    <RepeatButton Command="ScrollBar.PageRightCommand" Opacity="0" IsHitTestVisible="False"/>
                </Track.IncreaseRepeatButton>
            </Track>
        </Grid>
    </ControlTemplate>

    <Style TargetType="{x:Type ScrollBar}">
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="OverridesDefaultStyle" Value="True"/>
        <Style.Triggers>
            <Trigger Property="Orientation" Value="Vertical">
                <Setter Property="Template" Value="{StaticResource GC_ScrollBar_Vertical}"/>
                <Setter Property="Width" Value="10"/>
            </Trigger>
            <Trigger Property="Orientation" Value="Horizontal">
                <Setter Property="Template" Value="{StaticResource GC_ScrollBar_Horizontal}"/>
                <Setter Property="Height" Value="10"/>
            </Trigger>
        </Style.Triggers>
    </Style>
</Application.Resources>
</Application>
