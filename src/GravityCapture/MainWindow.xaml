<Window x:Class="GravityCapture.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Gravity Capture"
        MinWidth="1100" MinHeight="720"
        Loaded="Window_Loaded"
        Closing="Window_Closing"
        SourceInitialized="Window_SourceInitialized"
        SizeChanged="Window_SizeChanged">

    <!-- simple form spacing helpers -->
    <Window.Resources>
        <Style x:Key="FormLabel" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource TextMutedBrush}"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,0,8,8"/>
        </Style>
        <Style x:Key="FormField" TargetType="Control">
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="28"/>
        </Grid.RowDefinitions>

        <!-- Tabs -->
        <TabControl Grid.Row="0">
            <!-- Capture Tab -->
            <TabItem Header="Capture">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Toolbar that wraps when narrow -->
                    <Border Background="{DynamicResource CardColor}" Padding="8" Margin="8,8,8,0">
                        <WrapPanel VerticalAlignment="Center" ItemHeight="32" ItemWidth="Auto">
                            <TextBlock Text="Server" VerticalAlignment="Center" Margin="0,0,8,0"
                                       Foreground="{DynamicResource TextMutedBrush}"/>
                            <TextBox x:Name="ServerBox" Width="220"/>

                            <TextBlock Text="Tribe" VerticalAlignment="Center" Margin="16,0,8,0"
                                       Foreground="{DynamicResource TextMutedBrush}"/>
                            <TextBox x:Name="TribeBox" Width="300"/>

                            <Button x:Name="StartBtn" Content="Start" Margin="8,0,0,0" Click="StartBtn_Click"/>
                            <Button x:Name="StopBtn" Content="Stop" Margin="8,0,0,0" Click="StopBtn_Click"/>
                            <Button x:Name="SelectCropBtn" Content="Select Log Area..." Margin="8,0,0,0" Click="SelectCropBtn_Click"/>

                            <!-- OCR actions stay near toolbar; the WrapPanel keeps them visible -->
                            <Button x:Name="OcrCropBtn" Content="OCR Crop → Paste" Margin="8,0,0,0" Click="OcrCropBtn_Click"/>
                            <Button x:Name="OcrAndPostNowBtn" Content="OCR &amp; Post Visible Now" Margin="8,0,0,0" Click="OcrAndPostNowBtn_Click"/>

                            <Button x:Name="SaveDebugBtn" Content="Save debug ZIP" Margin="8,0,0,0" Click="SaveDebugBtn_Click"/>
                        </WrapPanel>
                    </Border>

                    <!-- Split content -->
                    <Grid Grid.Row="1" Margin="8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1.25*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Left: Live preview with optional OCR overlay -->
                        <GroupBox Header="Live preview" Grid.Column="0" Margin="0,0,8,0">
                            <Grid>
                                <Image x:Name="LivePreview" Stretch="Uniform" />
                                <!-- overlay (toggled from Settings) -->
                                <Border x:Name="OcrDetailsOverlay"
                                        Background="#80000000"
                                        Visibility="Collapsed"
                                        Margin="6" CornerRadius="6">
                                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                                        <TextBlock x:Name="OcrDetailsText" FontFamily="Consolas" FontSize="14"
                                                   Foreground="{DynamicResource TextBrush}" />
                                    </ScrollViewer>
                                </Border>
                            </Grid>
                        </GroupBox>

                        <!-- Right: API echo (top) + big paste box (bottom) -->
                        <Grid Grid.Column="1">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="2*"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <GroupBox Header="API echo" Grid.Row="0" Margin="0,0,0,8">
                                <TextBox x:Name="ApiEchoText"
                                         FontFamily="Consolas" FontSize="13"
                                         AcceptsReturn="True" VerticalScrollBarVisibility="Auto"
                                         IsReadOnly="True"/>
                            </GroupBox>

                            <GroupBox Header="Paste a tribe log line" Grid.Row="1">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="*"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <!-- BIG paste area -->
                                    <TextBox x:Name="LogLineBox"
                                             FontFamily="Consolas" FontSize="13"
                                             AcceptsReturn="True" VerticalScrollBarVisibility="Auto"
                                             MinHeight="140" />

                                    <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
                                        <Button x:Name="SendPastedBtn"
                                                Content="Send Pasted Log Line"
                                                Click="SendPastedBtn_Click"/>
                                    </StackPanel>
                                </Grid>
                            </GroupBox>
                        </Grid>
                    </Grid>
                </Grid>
            </TabItem>

            <!-- Settings Tab -->
            <TabItem Header="Settings">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="8" Orientation="Vertical">

                        <GroupBox Header="API credentials" Margin="0,0,0,8">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="180"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Text="Channel ID" Grid.Row="0" Grid.Column="0" Style="{StaticResource FormLabel}"/>
                                <TextBox x:Name="ChannelBox" Grid.Row="0" Grid.Column="1" Style="{StaticResource FormField}"/>

                                <TextBlock Text="API Base URL" Grid.Row="1" Grid.Column="0" Style="{StaticResource FormLabel}"/>
                                <TextBox x:Name="ApiUrlBox" Grid.Row="1" Grid.Column="1" Style="{StaticResource FormField}"/>

                                <TextBlock Text="API Key" Grid.Row="2" Grid.Column="0" Style="{StaticResource FormLabel}"/>
                                <PasswordBox x:Name="ApiKeyBox" Grid.Row="2" Grid.Column="1" Style="{StaticResource FormField}"/>
                            </Grid>
                        </GroupBox>

                        <GroupBox Header="Endpoint paths" Margin="0,0,0,8">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="180"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Text="OCR path:" Grid.Row="0" Grid.Column="0" Style="{StaticResource FormLabel}"/>
                                <TextBox x:Name="OcrPathBox" Grid.Row="0" Grid.Column="1" Style="{StaticResource FormField}"/>

                                <TextBlock Text="Screenshot ingest path:" Grid.Row="1" Grid.Column="0" Style="{StaticResource FormLabel}"/>
                                <TextBox x:Name="ScreenshotPathBox" Grid.Row="1" Grid.Column="1" Style="{StaticResource FormField}"/>

                                <TextBlock Text="Log-line ingest path:" Grid.Row="2" Grid.Column="0" Style="{StaticResource FormLabel}"/>
                                <TextBox x:Name="LogLinePathBox" Grid.Row="2" Grid.Column="1" Style="{StaticResource FormField}"/>
                            </Grid>
                        </GroupBox>

                        <GroupBox Header="Filters" Margin="0,0,0,8">
                            <UniformGrid Columns="2">
                                <CheckBox x:Name="FilterTameDeathsBox"            Content="Tame deaths"/>
                                <CheckBox x:Name="FilterTamesStarvingBox"          Content="Tames starving to death"/>
                                <CheckBox x:Name="FilterStructDestroyedBox"        Content="Structures destroyed"/>
                                <CheckBox x:Name="FilterStructAutoDecayBox"        Content="Structures auto decay"/>
                                <CheckBox x:Name="FilterTribeMateDeathsBox"        Content="Tribemate deaths"/>
                                <CheckBox x:Name="FilterTribeKillsEnemyTamesBox"   Content="Tribe kills enemy tames"/>
                                <CheckBox x:Name="FilterEnemyPlayerKillsBox"       Content="Enemy player kills"/>
                                <CheckBox x:Name="FilterTribeDemolishBox"          Content="Tribemates demolishing structures"/>
                                <CheckBox x:Name="FilterTribeFreezeTamesBox"       Content="Tribemates freezing tames"/>
                            </UniformGrid>
                        </GroupBox>

                        <GroupBox Header="Overlay" Margin="0,0,0,8">
                            <StackPanel Orientation="Horizontal">
                                <CheckBox x:Name="ShowOcrDetailsCheck"
                                          Content="Show OCR details overlay on Capture"
                                          Checked="ShowOcrDetailsCheck_Changed"
                                          Unchecked="ShowOcrDetailsCheck_Changed"/>
                            </StackPanel>
                        </GroupBox>

                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <Button x:Name="SaveBtn" Content="Save" Click="SaveBtn_Click"/>
                        </StackPanel>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>
        </TabControl>

        <!-- Status Bar -->
        <Border Grid.Row="1" Background="#22252B" Padding="6,4">
            <TextBlock x:Name="StatusText" Foreground="{DynamicResource TextMutedBrush}" />
        </Border>
    </Grid>
</Window>
