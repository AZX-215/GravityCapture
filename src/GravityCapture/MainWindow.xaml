<Window x:Class="GravityCapture.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Gravity Capture" Height="720" Width="1060"
        Background="#1E1F23"
        FontFamily="Segoe UI" Foreground="#EDEDED"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="4,2"/>
            <Setter Property="Padding" Value="6,4"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Margin" Value="4,2"/>
            <Setter Property="Padding" Value="6,4"/>
        </Style>
        <Style TargetType="CheckBox">
            <Setter Property="Margin" Value="6,2"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Margin" Value="6,4"/>
            <Setter Property="Padding" Value="10,6"/>
        </Style>

        <!-- Use local brushes for card look; corner radius now comes from App.xaml -->
        <SolidColorBrush x:Key="Card" Color="#2A2C31"/>
        <SolidColorBrush x:Key="CardBorder" Color="#393B42"/>
        <!-- Removed local CardRadius to avoid any type/key conflicts -->
    </Window.Resources>

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>  <!-- Top controls -->
            <RowDefinition Height="Auto"/>  <!-- Filters -->
            <RowDefinition Height="*"/>     <!-- Paste box -->
            <RowDefinition Height="Auto"/>  <!-- Bottom status -->
        </Grid.RowDefinitions>

        <!-- TOP: two-column card -->
        <Border Grid.Row="0" Background="{StaticResource Card}" BorderBrush="{StaticResource CardBorder}"
                BorderThickness="1" CornerRadius="{StaticResource CardRadius}" Padding="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Column 1: Environment / API / Discord / Interval -->
                <Grid Grid.Column="0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Orientation="Horizontal" Grid.Row="0">
                        <TextBlock Text="Log Env:" Margin="2,2,8,2"/>
                        <ComboBox x:Name="EnvBox" Width="140">
                            <ComboBoxItem Content="Stage"/>
                            <ComboBoxItem Content="Prod"/>
                        </ComboBox>
                        <TextBlock Text="Channel ID:" Margin="16,2,8,2"/>
                        <TextBox x:Name="ChannelBox" Width="220"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Grid.Row="1">
                        <TextBlock Text="API URL:" Margin="2,2,8,2"/>
                        <TextBox x:Name="ApiUrlBox" Width="360"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Grid.Row="2">
                        <TextBlock Text="API Key:" Margin="2,2,8,2"/>
                        <TextBox x:Name="ApiKeyBox" Width="360"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Grid.Row="3">
                        <TextBlock Text="Interval (min):" Margin="2,2,8,2"/>
                        <TextBox x:Name="IntervalBox" Width="60" TextAlignment="Center"/>
                        <CheckBox x:Name="ActiveWindowCheck" Content="Capture active window only" Margin="16,0,0,0"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Grid.Row="4" VerticalAlignment="Center">
                        <TextBlock Text="JPEG Quality:" Margin="2,2,8,2"/>
                        <Slider x:Name="QualitySlider" Minimum="50" Maximum="100" Width="180" Value="85"/>
                        <TextBlock x:Name="QualityLabel" Text="85" Margin="8,0,0,0"/>
                    </StackPanel>
                </Grid>

                <!-- Column 2: Server / Tribe & Actions -->
                <Grid Grid.Column="1">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Orientation="Horizontal" Grid.Row="0">
                        <TextBlock Text="Server:" Margin="2,2,8,2"/>
                        <TextBox x:Name="ServerBox" Width="260"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Grid.Row="1">
                        <TextBlock Text="Tribe:" Margin="2,2,8,2"/>
                        <TextBox x:Name="TribeBox" Width="260"/>
                    </StackPanel>

                    <!-- Hidden, retained for code-behind compatibility -->
                    <TextBox x:Name="TitleHintBox" Visibility="Collapsed" />

                    <WrapPanel Grid.Row="2" Margin="0,8,0,0">
                        <Button x:Name="SaveBtn" Click="SaveBtn_Click" Content="Save" />
                        <Button x:Name="StartBtn" Click="StartBtn_Click" Content="Start" />
                        <Button x:Name="StopBtn" Click="StopBtn_Click" Content="Stop" />
                        <Button x:Name="RefreshBtn" Click="RefreshRecent_Click" Content="Refresh Recent" />
                        <Button x:Name="SelectAreaBtn" Click="SelectCropArea_Click" Content="Select Log Area…" />
                        <Button x:Name="PreviewCropBtn" Click="PreviewCrop_Click" Content="Preview Crop" />
                    </WrapPanel>

                    <WrapPanel Grid.Row="3" Margin="0,6,0,0">
                        <Button x:Name="OcrCropBtn" Click="OcrCropPaste_Click" Content="OCR Crop → Paste" />
                        <Button x:Name="OcrAndPostNowBtn" Click="OcrAndPostNow_Click" Content="OCR &amp; Post Visible Now" />
                        <Button x:Name="SendTestBtn" Click="SendTestTribeEvent_Click" Content="Send Test Tribe Event" />
                        <Button x:Name="SendPastedBtn" Click="SendPastedLine_Click" Content="Send Pasted Log Line" />
                    </WrapPanel>
                </Grid>
            </Grid>
        </Border>

        <!-- FILTERS -->
        <Border Grid.Row="1" Background="{StaticResource Card}" BorderBrush="{StaticResource CardBorder}"
                BorderThickness="1" CornerRadius="{StaticResource CardRadius}" Padding="10" Margin="0,10,0,0">
            <StackPanel Orientation="Horizontal">
                <CheckBox x:Name="AutoOcrCheck" Content="Auto OCR ingest"/>
                <CheckBox x:Name="RedOnlyCheck" Content="Post only red logs" Margin="24,2,0,2"/>
                <TextBlock Text="Filters:" Margin="24,2,8,2"/>
                <CheckBox x:Name="FilterTameCheck" Content="Tame deaths"/>
                <CheckBox x:Name="FilterStructCheck" Content="Structures destroyed"/>
                <CheckBox x:Name="FilterTribeCheck" Content="Tribemate deaths"/>
            </StackPanel>
        </Border>

        <!-- PASTE BOX -->
        <Border Grid.Row="2" Background="{StaticResource Card}" BorderBrush="{StaticResource CardBorder}"
                BorderThickness="1" CornerRadius="{StaticResource CardRadius}" Padding="10" Margin="0,10,0,0">
            <DockPanel>
                <TextBlock Text="Paste a tribe log line:" DockPanel.Dock="Top" Margin="2,0,0,6"/>
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <TextBox x:Name="LogLineBox" AcceptsReturn="True" FontFamily="Consolas"
                             Foreground="#EEEEEE" Background="#111214" BorderThickness="0"
                             HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" />
                </ScrollViewer>
            </DockPanel>
        </Border>

        <!-- STATUS -->
        <DockPanel Grid.Row="3" Margin="0,10,0,0">
            <TextBlock x:Name="StatusText" Foreground="#9ECFFF" />
        </DockPanel>
    </Grid>
</Window>
