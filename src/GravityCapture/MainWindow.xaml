<Window x:Class="GravityCapture.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Gravity Capture"
        MinWidth="1100" MinHeight="720"
        Loaded="Window_Loaded"
        Closing="Window_Closing"
        SourceInitialized="Window_SourceInitialized"
        SizeChanged="Window_SizeChanged">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="28"/>
        </Grid.RowDefinitions>

        <!-- Tabs -->
        <TabControl Grid.Row="0">
            <!-- ======================= Capture ======================= -->
            <TabItem Header="Capture">
                <Grid>
                    <Grid.RowDefinitions>
                        <!-- Banner -->
                        <RowDefinition Height="Auto"/>
                        <!-- Toolbar -->
                        <RowDefinition Height="Auto"/>
                        <!-- Main content -->
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Banner just below the tab header -->
                    <Border Grid.Row="0" Background="{DynamicResource CardColor}" Margin="0,0,0,6">
                        <Image Height="56"
                               Stretch="Uniform"
                               VerticalAlignment="Center"
                               Source="pack://application:,,,/GravityCapture;component/Assets/Gravity_Capture_App_Banner.png"/>
                    </Border>

                    <!-- Toolbar (wraps when narrow) -->
                    <Border Grid.Row="1" Background="{DynamicResource CardColor}" Padding="8" Margin="8,0,8,0">
                        <WrapPanel VerticalAlignment="Center" ItemHeight="32">
                            <TextBlock Text="Server" VerticalAlignment="Center" Margin="0,0,8,0"
                                       Foreground="{DynamicResource TextMutedBrush}"/>
                            <TextBox x:Name="ServerBox" Width="220"/>

                            <TextBlock Text="Tribe" VerticalAlignment="Center" Margin="16,0,8,0"
                                       Foreground="{DynamicResource TextMutedBrush}"/>
                            <TextBox x:Name="TribeBox" Width="300"/>

                            <Button x:Name="StartBtn" Content="Start" Click="StartBtn_Click"/>
                            <Button x:Name="StopBtn" Content="Stop" Click="StopBtn_Click"/>
                            <Button x:Name="SelectCropBtn" Content="Select Log Area..." Click="SelectCropBtn_Click"/>

                            <Button x:Name="OcrCropBtn" Content="OCR Crop → Paste" Click="OcrCropBtn_Click"/>
                            <Button x:Name="OcrAndPostNowBtn" Content="OCR &amp; Post Visible Now" Click="OcrAndPostNowBtn_Click"/>

                            <Button x:Name="SaveDebugBtn" Content="Save debug ZIP" Click="SaveDebugBtn_Click"/>
                        </WrapPanel>
                    </Border>

                    <!-- Split content -->
                    <Grid Grid.Row="2" Margin="8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="5*"/>
                            <ColumnDefinition Width="4*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Left: Live preview with optional OCR overlay -->
                        <GroupBox Header="Live preview" Grid.Column="0" Margin="0,0,8,0">
                            <Grid>
                                <Image x:Name="LivePreview" Stretch="Uniform"/>
                                <!-- overlay (toggled from Settings) -->
                                <Border x:Name="OcrDetailsOverlay"
                                        Background="#80000000"
                                        Visibility="Collapsed"
                                        Margin="6" CornerRadius="6">
                                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                                        <TextBlock x:Name="OcrDetailsText"
                                                   FontFamily="Consolas" FontSize="14"
                                                   Foreground="{DynamicResource TextBrush}"/>
                                    </ScrollViewer>
                                </Border>
                            </Grid>
                        </GroupBox>

                        <!-- Right: API echo (top) + big paste box (bottom) -->
                        <Grid Grid.Column="1">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="2*"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <GroupBox Header="API Echo" Grid.Row="0" Margin="8,0,0,8">
                                <TextBox x:Name="ApiEchoText"
                                         FontFamily="Consolas" FontSize="13"
                                         AcceptsReturn="True" VerticalScrollBarVisibility="Auto"
                                         IsReadOnly="True"/>
                            </GroupBox>

                            <GroupBox Header="Tribe Log Text Output Testing" Grid.Row="1" Margin="8,0,0,0">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="*"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <TextBox x:Name="LogLineBox"
                                             FontFamily="Consolas" FontSize="13"
                                             AcceptsReturn="True" VerticalScrollBarVisibility="Auto"
                                             MinHeight="120"/>

                                    <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
                                        <Button x:Name="SendPastedBtn"
                                                Content="Send Pasted Logs"
                                                Click="SendParsedBtn_Click"/>
                                    </StackPanel>
                                </Grid>
                            </GroupBox>
                        </Grid>
                    </Grid>
                </Grid>
            </TabItem>

            <!-- ======================= Settings ======================= -->
            <TabItem Header="Settings">
                <Grid>
                    <Grid.RowDefinitions>
                        <!-- Banner -->
                        <RowDefinition Height="Auto"/>
                        <!-- Settings body -->
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Banner -->
                    <Border Grid.Row="0" Background="{DynamicResource CardColor}" Margin="0,0,0,6">
                        <Image Height="56"
                               Stretch="Uniform"
                               VerticalAlignment="Center"
                               Source="pack://application:,,,/GravityCapture;component/Assets/Gravity_Capture_App_Banner.png"/>
                    </Border>

                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                        <StackPanel Margin="8" Orientation="Vertical">

                            <GroupBox Header="API credentials">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="180"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Text="Channel ID" Grid.Row="0" Grid.Column="0"
                                               Foreground="{DynamicResource TextMutedBrush}"/>
                                    <TextBox x:Name="ChannelBox" Grid.Row="0" Grid.Column="1"/>

                                    <TextBlock Text="API Base URL" Grid.Row="1" Grid.Column="0"
                                               Foreground="{DynamicResource TextMutedBrush}"/>
                                    <TextBox x:Name="ApiUrlBox" Grid.Row="1" Grid.Column="1"/>

                                    <TextBlock Text="API Key" Grid.Row="2" Grid.Column="0"
                                               Foreground="{DynamicResource TextMutedBrush}"/>
                                    <PasswordBox x:Name="ApiKeyBox" Grid.Row="2" Grid.Column="1"/>
                                </Grid>
                            </GroupBox>

                            <GroupBox Header="Endpoint paths">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="180"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Text="OCR path:" Grid.Row="0" Grid.Column="0"
                                               Foreground="{DynamicResource TextMutedBrush}"/>
                                    <TextBox x:Name="OcrPathBox" Grid.Row="0" Grid.Column="1"/>

                                    <TextBlock Text="Screenshot ingest path:" Grid.Row="1" Grid.Column="0"
                                               Foreground="{DynamicResource TextMutedBrush}"/>
                                    <TextBox x:Name="ScreenshotPathBox" Grid.Row="1" Grid.Column="1"/>

                                    <TextBlock Text="Log-line ingest path:" Grid.Row="2" Grid.Column="0"
                                               Foreground="{DynamicResource TextMutedBrush}"/>
                                    <TextBox x:Name="LogLinePathBox" Grid.Row="2" Grid.Column="1"/>
                                </Grid>
                            </GroupBox>

                            <GroupBox Header="Filters">
                                <UniformGrid Columns="2">
                                    <CheckBox x:Name="FilterTameDeathsBox"            Content="Tame deaths"/>
                                    <CheckBox x:Name="FilterTamesStarvingBox"          Content="Tames starving to death"/>
                                    <CheckBox x:Name="FilterStructDestroyedBox"        Content="Structures destroyed"/>
                                    <CheckBox x:Name="FilterStructAutoDecayBox"        Content="Structures auto decay"/>
                                    <CheckBox x:Name="FilterTribeMateDeathsBox"        Content="Tribemate deaths"/>
                                    <CheckBox x:Name="FilterTribeKillsEnemyTamesBox"   Content="Tribe kills enemy tames"/>
                                    <CheckBox x:Name="FilterEnemyPlayerKillsBox"       Content="Enemy player kills"/>
                                    <CheckBox x:Name="FilterTribeDemolishBox"          Content="Tribemates demolishing structures"/>
                                    <CheckBox x:Name="FilterTribeFreezeTamesBox"       Content="Tribemates freezing tames"/>
                                </UniformGrid>
                            </GroupBox>

                            <GroupBox Header="Overlay">
                                <StackPanel Orientation="Horizontal">
                                    <CheckBox x:Name="ShowOcrDetailsCheck"
                                              Content="Show OCR details overlay on Capture"
                                              Checked="ShowOcrDetailsCheck_Changed"
                                              Unchecked="ShowOcrDetailsCheck_Changed"/>
                                </StackPanel>
                            </GroupBox>

                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                <Button x:Name="SaveBtn" Content="Save" Click="SaveBtn_Click"/>
                            </StackPanel>
                        </StackPanel>
                    </ScrollViewer>
                </Grid>
            </TabItem>
        </TabControl>

        <!-- Status Bar -->
        <Border Grid.Row="1" Background="#22252B" Padding="6,4">
            <TextBlock x:Name="StatusText" Foreground="{DynamicResource TextMutedBrush}"/>
        </Border>
    </Grid>
</Window>
